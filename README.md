python --version

pip --version

pip install --upgrade pip

python -m django --version

pip install django

python -m django --version

pip install mysqlclient

pip install Pillow

python manage.py runserver

